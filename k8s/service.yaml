apiVersion: v1
kind: Service
metadata:
  name: wikipulse-metrics-service
spec:
  selector:
    app: wikipulse-worker
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
